<!doctype html>
<html>
<head>
<meta charset="utf-8">
	<!-- 通過為viewport meta標籤添加user-scalable=no可以禁用其縮放（zooming）功能。 -->
	<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
	<!-- Bootstrap -->
	<link rel="stylesheet" href="/css/bootstrap.css">
	<link rel="stylesheet" href="/css/main.css">
	<!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
	<!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
	<!--[if lt IE 9]>
	<script src="js/html5shiv.min.js"></script>
	<script src="js/respond.min.js"></script>
	<![endif]-->
	<meta name="Robots" content="index,follow"/>
	<meta http-equiv="content-language" content="zh-TW" />
	<meta name="keywords" content="關鍵字"/>
	<meta name="description" content="簡介"/>
	<meta name="abstract" content="摘要"/>
	<link rel="image_src" href="http://網址/fb.jpg"/>
	<meta property="og:title" content="ebs"/>
	<meta property="og:description" content="簡介"/>
	<meta property="og:site_name" content="站名"/>
	<meta property="og:image" content="http://網址/fb.jpg"/>
	<meta name="DC.Rights" content="ebs"/>
	<meta name="DC.Date" content="2014-4-27"/>
<title>EBS TEMP FRONT</title>

	<%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
	  <script>
  	window.onload= function() {
	  <% if !flash[:notice].nil? %>
	   alertify['success']('<%= flash[:notice] %>', 3000);
	  <% end %>
	  
	  <% if !flash[:warning].nil? %>
	  alertify['error']("<%= flash[:warning].html_safe %>", 3000); 
	  <% end %>

		$('a').click(function() {
			if( window.location.toString().match(/page=\d+/) ) { 
				document.cookie = ( 'last_page_num=' + window.location.toString().match(/page=\d+/)[0].split('=')[1] + ';' );

			}
		})
		
		$(window).bind('peditor-updated', function() {
			
			var main = $('section article');
			
			if ( (main.innerHeight() + 100) > $('[_height=auto]').height() ) {
				$('[_height=auto]').height('inherit');
			} else {
				fullHeight();
			}
			
		});
		
	}

  </script>
</head>

<body>
			<%= yield %>
<!--main-->
<script src="/js/bootstrap.min.js"></script>
</body>
</html>
